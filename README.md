# go-kerberos

`go-kerberos` is a pure Go Kerberos V5 implementation targeting wire,
cryptographic, file-format, and behavioral interoperability with MIT krb5.
It is tests-first, uses no cgo or gokrb5, and keeps the production library
independent of MIT Kerberos at runtime.

**Status: Phase 0 — client test architecture scaffold.**
