# Packet fixtures

Fixtures must document source, MIT krb5 version or RFC section, command used,
principal, realm, enctype, and expected interpretation. Sensitive keys and
credentials must not be committed.

AS-REQ/AS-REP and later packet captures remain future work. Tests must skip
with `fixture not yet generated` until captures are produced by the MIT
interop harness.
