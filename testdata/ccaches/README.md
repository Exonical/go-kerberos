# Credential-cache fixtures

Fixtures must document source, MIT krb5 version, command used, principal,
realm, enctype, and expected interpretation. Use synthetic test credentials
only and prefer deterministic vectors.
