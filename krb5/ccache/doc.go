// Package ccache reads and writes MIT-compatible credential caches.
//
// Supported backends are FILE and DIR caches, process-local MEMORY caches,
// Heimdal KCM caches, Linux KEYRING caches, and Windows MSLSA caches.
package ccache
